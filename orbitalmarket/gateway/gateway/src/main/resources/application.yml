server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: payments
          uri: http://localhost:8081
          predicates:
            - Path=/api/v1/payments/**
        - id: orders
          uri: http://localhost:8082
          predicates:
            - Path=/api/v1/orders/**